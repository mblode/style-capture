pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "**/*.{js,ts,jsx,tsx}"
      run: npx oxlint --fix {staged_files}
      stage_fixed: true
    - name: format
      glob: "**/*.{js,ts,jsx,tsx,json,jsonc}"
      run: npx oxfmt --write {staged_files}
      stage_fixed: true
